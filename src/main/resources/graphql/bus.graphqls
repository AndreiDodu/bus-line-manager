type Bus {

    id: ID!
    capacity: Int!
}

input BusInput {
    capacity: Int!
}

type Query {
    getBus(id: ID!): Bus
	getBuses: [Bus]
}

type Mutation{
	insertBus(data: BusInput!): Bus!
	updateBus(id: ID!, data: BusInput!): Bus!
}